__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/
build/
dist/

# 개인 데이터는 커밋하지 않는다
data/market.csv
data/state.json
data/manual_input.yaml
journal/20*.md
!journal/TEMPLATE.md

# 백테스트 보고서는 커밋한다 (reports/ 자체는 유지)
# 시각화 중간 산출물 — 완성된 페이지(viz/bcs-gauge.html)에 같은 데이터가 이미
# 들어 있다. tools/export_viz.py 로 언제든 다시 만든다.
reports/viz-data.json
